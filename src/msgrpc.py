__author__ = 'dave'

class msgrpc:
    def __init__(self, host, port, username, password):
        self.host = str(host)
        self.port = int(55552)
        self.username = str(username)
        self.password = str(password)
        